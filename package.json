{
  "name": "spittle-server",
  "version": "1.0.0",
  "description": "Spittle Multiplayer Broker and Relay Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.1",
    "ws": "^8.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

